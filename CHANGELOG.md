# Changelog

All notable changes to CLAVIS will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Project foundation with Maven multi-module structure
- Core shared library (`clavis-core`) with config, HTTP, rate limiting, caching
- PubMed MCP Server (`clavis-pubmed`)
- Europe PMC MCP Server (`clavis-europepmc`)
- Semantic Scholar MCP Server (`clavis-semanticscholar`)
- arXiv MCP Server (`clavis-arxiv`)
- ClinicalTrials.gov MCP Server (`clavis-clinicaltrials`)
- DrugBank MCP Server (`clavis-drugbank`)
- PubChem MCP Server (`clavis-pubchem`)
- UniProt MCP Server (`clavis-uniprot`)
- KEGG MCP Server (`clavis-kegg`)
- Reactome MCP Server (`clavis-reactome`)
- Comprehensive documentation and contributing guides
